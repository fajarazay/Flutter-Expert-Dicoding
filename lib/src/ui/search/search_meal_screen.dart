import 'package:flutter/material.dart';
import 'package:flutter_expert_dicoding/src/blocs/search_meal_bloc.dart';
import 'package:flutter_expert_dicoding/src/blocs/search_meal_provider.dart';

class SearchMealScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
   // final SearchMealBloc bloc = SearchMealProvider.of(context);
    //bloc.fetchAllDataSearchMeals(meal.idMeal);
    return Center(
      child: Text("Test"),
    );
  }
}
